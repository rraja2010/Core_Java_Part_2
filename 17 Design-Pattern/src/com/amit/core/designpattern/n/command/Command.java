package com.amit.core.designpattern.n.command;

public interface Command {
	public void execute();
}

/*
This command interface will be implemented by as many commands as we want.
In our case we have two commands.

Create the first one which is the on command on command  and Off command.
*/