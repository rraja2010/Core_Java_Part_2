enum A 
{

	/*

1. Enumeration concept is introduced in 1.5 version
2. Enumeration is used to declare group of named constant s.
3. We are declaring the enum by using enum keyword. For the enums the compiler will generate .classes
4. enum is a keyword and Enum is a class and every enum is direct child class of java.lang.Enum 
	so it is not possible to inherit the some other class. Hence for the enum inheritance concept is not applicable
5. by default enum constants are public static final
	  
	 */
}
