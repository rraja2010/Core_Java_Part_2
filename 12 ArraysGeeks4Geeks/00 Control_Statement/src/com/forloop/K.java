package com.forloop;
// inside the for loop each and every part is optional
public class K {
	public static void main(String[] args) {
		for (;;) { // represent infinite loop because the condition is always true
			System.out.println("inside the for loop each and every part is optional");
		}
	}
}
